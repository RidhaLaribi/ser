{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "dependencies": {
    "@mistralai/mistralai": "^latest",
    "express": "^latest",
    "cors": "^latest",
    "dotenv": "^latest",
    "zod": "^latest"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm install zod"
  }
}